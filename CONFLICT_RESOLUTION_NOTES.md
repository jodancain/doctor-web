# 痛风管家最新改动说明

本次在现有云开发版本的基础上，完成了以下整理与冲突修复工作：

1. **云函数二维码路径对齐**：`healthDataService` 的 `getMiniProgramCode` 入口已改为生成首页（`pages/home/index`）的小程序码，避免旧路径导致二维码无法打开。
2. **文档指引同步**：更新《CLOUD_FUNCTION_USAGE.md》，将接口名称修正为实际使用的 `getDashboardData`，同时补充返回值概述，确保前后端开发在调用数据看板接口时无歧义。
3. **仓库冲突清理**：移除部署过程遗留的 `node_modules` 目录并确认仓库无冲突文件，便于继续在当前分支上开发与提测。

> ✅ 建议在上传云函数后使用微信开发者工具重新生成一次二维码，确认最新路径生效；文档也已说明七日汇总接口的返回字段，可直接用于联调。
